# Sharm Chetty — Portfolio

Plain HTML/CSS/JS site for the ITPP5112 Practical Assignment 1 brief.
No build tools — open the folder in VS Code and use the "Live Server" extension
(or just double-click index.html) to preview.

## Structure
```
index.html              Home
pages/about.html        About Me
pages/projects.html     Projects
pages/reflection.html   Professional Reflection
pages/contact.html      Contact
css/style.css           All styling (colour variables at the top)
js/script.js            Mobile nav toggle
assets/                 Images — currently placeholders, see TODOs below
```

## Before you submit — TODO checklist
- [ ] Replace `assets/profile-photo.jpg` with a real, professional photo
- [ ] Replace `assets/project-mobile-app.jpg` and `assets/project-logic-exercises.jpg`
      with real screenshots of your work
- [ ] Add your real GitHub URL in `pages/projects.html` (both project cards) and
      `pages/contact.html`
- [ ] Add your real LinkedIn URL in `pages/contact.html`
- [ ] Rewrite the marked <span class="todo">TODO</span> paragraphs in
      `pages/about.html` and `pages/reflection.html` in your own words — the
      reflection especially needs to be genuinely yours, not a template
- [ ] Add at least one more project if you have one, or confirm these two are fine
- [ ] Search the page source for "TODO" to make sure you haven't missed one
- [ ] Check every link and image works, then publish via GitHub Pages
      (Settings → Pages → deploy from the `main` branch)

## Deploying to GitHub Pages
1. Create a new GitHub repo (e.g. `portfolio` or `<username>.github.io`)
2. Push this folder's contents to it
3. Go to the repo's Settings → Pages → set source to the `main` branch, root folder
4. Your site will be live at `https://<username>.github.io/<repo-name>/`
   (or `https://<username>.github.io/` if you used the `<username>.github.io` repo name)
